// return an array of n nested arrays (examples below)
var buildArray = function (n) {

};

buildArray(0); // => []
buildArray(1); // => [[]]
buildArray(2); // => [[[]]]
buildArray(3); // => [[[[]]]]
